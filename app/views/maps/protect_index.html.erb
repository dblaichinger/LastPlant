<h1>Protect</h1>
<p>session: <%= session %>
</p>
<p>session id: <%= session[:id] %></p>
<p>session id komisch: <%= session['id'] %></p>
<p>current user: <%= current_user.id %></p>
<ul>
<% if @maps %>
    <% @maps.each do |map| 
    %>
    <li><%= map.name %> | 
    <%= link_to "delete", map, :method => :delete, :confirm => "Are you sure to delete #{map.name}?", :title => "Delete #{map.name}"%>
    </li>
    <% end 
    %>
<% end %>
<p><%= link_to "New map", new_map_path %></p>
</ul>


<h1>CMD: <% if(@cmd) %>
<%=@cmd%>
<% end %>

</h1>
